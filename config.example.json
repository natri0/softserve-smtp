{
  "_comment": [
    "move this file to server/config/config.json or else it won't start",
    "all values here are the defaults"
  ],

  "port": 12345,
  "thread_pool_size": 4,

  "smtp": {
    "domain": "smtp.test"
  }
}